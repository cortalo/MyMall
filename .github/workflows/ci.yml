name: CI

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "**" ]

jobs:
  ci:
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: oomall_test
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -proot"
          --health-interval=5s
          --health-timeout=5s
          --health-retries=10

    steps:
      - name: 拉取代码
        uses: actions/checkout@v4

      - name: 安装 Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: 初始化数据库
        run: mysql -h 127.0.0.1 -uroot -proot oomall_test < scripts/init.sql

      - name: 运行测试
        run: go test ./...

      - name: 构建验证
        run: go build ./...

#      - name: 配置 AWS 凭证
#        uses: aws-actions/configure-aws-credentials@v4
#        with:
#          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#          aws-region: eu-central-1
#
#      - name: 登录 ECR
#        id: login-ecr
#        uses: aws-actions/amazon-ecr-login@v2
#
#      - name: 构建并推送镜像
#        run: |
#          IMAGE_URI=288639564595.dkr.ecr.eu-central-1.amazonaws.com/mymall
#          docker build -t $IMAGE_URI:${{ github.sha }} -t $IMAGE_URI:latest .
#          docker push $IMAGE_URI:${{ github.sha }}
#          docker push $IMAGE_URI:latest
#
#      - name: 更新 Task Definition
#        id: task-def
#        uses: aws-actions/amazon-ecs-render-task-definition@v1
#        with:
#          task-definition: task-def.json
#          container-name: app
#          image: 288639564595.dkr.ecr.eu-central-1.amazonaws.com/mymall:${{ github.sha }}
#
#      - name: 部署到 ECS
#        uses: aws-actions/amazon-ecs-deploy-task-definition@v2
#        with:
#          task-definition: ${{ steps.task-def.outputs.task-definition }}
#          service: mymall-service
#          cluster: mymall-cluster
#          wait-for-service-stability: true